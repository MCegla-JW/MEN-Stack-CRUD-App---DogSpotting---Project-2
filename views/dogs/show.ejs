<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= dog.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link rel="stylesheet" href="/stylesheets/forms.css">
</head>
<body class="show">
        <nav class="navbar">
        <div class="navdiv">
            <div class="nav-left">
                <img class="app-logo" src="/assets/images/App-Logo.png" alt="image of a brown dog with a bone">
                <div class="app-name">
                    <span>DOG</span>
                    <span>SPOTTING</span>
                </div>
            </div>
            <div class="nav-right">
                <%- include('../partials/_navbar.ejs') %>
                <a href="/auth/sign-out"><button class="btn">Sign Out</button></a>
            </div>
        </div>
    </nav>
 <main>
    <div class="dog-profile">
    <img class="dog-placeholder" src="/assets/images/placeholder.png" alt="outline of a dog with a bone">
    <h1><%= dog.name %></h1>
    <h5><%= dog.breed %></h5>
    <p><%= dog.age %></p>
    <p><%= dog.location %></p>
    <p><%= dog.description %></p>
    </div>
    <% if (user && dog.owner._id.equals(user._id)) { %> 
    <div class="controls">
        <form>
       <button class="btn"><a href="/dogs/<%= dog._id %>/edit"> Edit <%= dog.name %></a></button>
    </form> 
    <form action="/dogs/<%=dog._id%>?_method=DELETE" method="POST">
        <button class="btn" type="submit"> Delete <%= dog.name %></button>
    </form>  
    </div>
    <% } %>
    <form>
     <button class="btn"><a href="/dogs"> Back </a></button>
     </form>
 </main>   
</body>
</html>